import 'package:flutter/material.dart';

class Lost extends StatelessWidget {
  const Lost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Página de Perdidos'),
      ),
    );
  }
}
