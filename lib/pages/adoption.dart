import 'package:flutter/material.dart';

class Adoption extends StatelessWidget {
  const Adoption({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Página de Adopción'),
      ),
    );
  }
}
